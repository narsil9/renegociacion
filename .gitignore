node_modules/
dist/
.env
.DS_Store
.vscode/
outputs/*.png
outputs/*.html
outputs/*.pdf
outputs/*.log
outputs/*.txt
outputs/acreditaciones_tmp/
docs_paso3/
documentos_descargados/
scratch/
informe_deudas_*.pdf
src/automation/html_samples/

# Casos de clientes reales (test scaffolding + PDFs + screenshots con datos personales) — no van al repo
casos/
# Carpetas de documentos de clientes sueltas
Abigail Cornejo Abarca*/
JORGE ANDRES ROMERO*/
# Salidas de corridas (screenshots/HTML/PDF/logs) — solo se versiona el placeholder .gitkeep
outputs/*
!outputs/.gitkeep
outputs/verify_tmp/

# Screenshots/PDFs sueltos en el root (datos de clientes, no van a git)
/Captura de pantalla*.png
/*.png
/*.pdf

# Bitácoras de sesión (scratch, no forman parte del código)
CHANGES_MADE.md

# Codex / agentes externos
.codex/
AGENTS.md

# tools/ = scripts dev/diagnóstico/one-off — NO producción. Solo se versiona la
# herramienta de validación del Paso 3 (batería determinista + fixtures).
tools/*
!tools/paso3_validacion/
src/utils/verify_*.ts

# Artefactos del arnés de validación (salidas del Centinela + cache LLM con datos de deuda)
_validacion/
casos/*/_validacion/
